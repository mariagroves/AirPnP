<div class="container">
  <div class="row d-flex justify-content-center">
    <div class="show-title">
      <h1> <%= @toilet.title %> </h1>
    </div>
  </div>
  <div class="row">
    <div class="col-sm-8 mb-4">
      <div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
        <div class="carousel-inner">
          <% @toilet.images.each_with_index do |image, index| %>
            <% if index == 0 %>
              <div class="carousel-item active">
                <img class="d-block" src="<%= cl_image_path image.url %>" alt="First slide">
              </div>
            <% else %>
              <div class="carousel-item">
                <img class="d-block" src="<%= cl_image_path image.url %>" alt="First slide">
              </div>
            <% end %>
          <% end %>
        </div>
        <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
          <span class="carousel-control-prev-icon" aria-hidden="true"></span>
          <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
          <span class="carousel-control-next-icon" aria-hidden="true"></span>
          <span class="sr-only">Next</span>
        </a>
      </div>

      <div class="show-card d-flex align-items-center justify-content-between">
        <div class="sub-title">
          <h3>This toilet in <%= @toilet.location %> is hosted by <%= @toilet.owner.first_name%> <%= @toilet.owner.last_name %></h3>
        </div>
        <div>
          <img src="https://blog.fromjapan.co.jp/en/wp-content/uploads/2015/08/Poop_2.jpg" alt="" class="avatar-large">
        </div>
      </div>

      <div class="show-card">
        <h3>Details</h3>
        <ul>
          <i class="fas fa-euro-sign"></i><%= @toilet.price %> per use
          <% @toilet.description.split(',').each do |description| %>
            <li class="d-flex">
              <div>
                <i class="fas fa-toilet mr-2"></i>
              </div>
              <%= description %>
            </li>
          <% end %>
        </ul>
      </div>

    </div>

    <div class="col-sm-4 booking-card">
      <%= simple_form_for [@toilet, @booking] do |f| %>
        <%= f.input :date %>
        <%= f.input :time %>
        <%= f.submit "Book!" %>
      <% end %>
    </div>
  </div>
</div>


